export enum ButtonVariant {
  PRIMARY = "primary",
  SECONDARY = "secondary",
  LINK = "link",
  DANGER = "danger",
  CUSTOM = "custom",
}

export enum ButtonSize {
  CUSTOM = "custom",
  SMALL = "small",
  MIDDLE = "middle",
  DEFAULT = "default",
}
